import SettingsScreenComponent from "@/app/componets/Settings/SettingsScreenComponent";
import { View } from "react-native";

export default function SettingsScreen() {
    return (
        <View style={{ flex: 1 }}>
            <SettingsScreenComponent />
        </View>
    )
}